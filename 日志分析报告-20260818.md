# ctf-agent 一晚日志分析报告（2026-08-18 凌晨，用户睡觉期间自动分析）

分析范围: /home/stw/ctf-agent/challenges/ 下全部 work_dir 日志
时间范围: 昨晚 08-17 20:00 之后实际运行的 63 题（目录内还有 15 个历史题 07-30/31 的）

## 一、总体成绩

| 指标 | 数值 |
|------|------|
| 昨晚题数 | 63 |
| 解出 | 59（94%） |
| 未解出 | 4 |
| 平均 codex 日志 | 57KB |
| 平均轮数 | 1.3（大多一轮解出） |

## 二、未解出的 4 题（VPN 恢复后可重试）

1. **a-03** (manual_web_8c7f95c36c19)：资产管理系统，认证绕过。崩溃重试 3 次，M22 分布法三轮全灭，爆破面判死；last 在 /download oracle + /report/export SSRF
2. **f2-05** (manual_binary_1af53940224a)：嵌入式授权引擎 VM 逆向。blob@0x4020 解密 + 密钥反推（已知前缀 flag{），崩溃重试 2 次
3. **manual_web_7e57350a396f**：Serverless console。已拿到 admin_token + FastCGI 9000 RCE 思路（exploit-admin 阶段），崩溃 2 次打断——**最接近解出**
4. **manual_web_2f4eb29e8d6b**：官网渗透（外网→内网）。4 分支枚举中（OA 路径 + auth.php 参数矩阵），进行中被中断

共同点: 都是"进行中被崩溃/中断打断"，不是系统性卡死。

## 三、发现的问题（按严重度）

### 1. 🔴 Hermes monitor 死锁（新发现，已修复）
- 现象: 8 题 hermes.log 只有 1 条"被触发"标记，之后 Hermes 监督完全死亡（codex 裸跑）
- 根因: run.sh monitor loop 的 `hermes chat` **无 timeout**——deepseek API 偶发挂起时，`RESP=$(hermes chat ...)` 永久阻塞，while 循环卡死，后续轮询全停
- 影响: 8 题（13%）Hermes 监督缺席；7 题 codex 仍独立解出（监督是增强不是依赖）
- 修复: 三处 hermes chat（monitor 自建 session / monitor 复用 session / warmup）全部加 `timeout 300`

### 2. 🟡 "No tool output found" 工具往返崩溃（已修复 r8）
- 现象: 9 题出现（各 2 次连续）
- 影响: 8 题最终解出（codex 自愈/重试成功），1 题未解出（7e57350a396f 叠加中断）
- 修复: codex exec 同轮快速重试 3 次（deepseek API 兼容层偶发，无法根治，重试兜底）

### 3. 🟡 上下文丢失链（已修复，多文件）
- run.sh 无条件重置 progress/board → IS_RESUME 续跑
- master.py unlink progress.md 与续跑冲突 → 删除 unlink
- Hermes 预热无条件初始化 board → 续跑语义 + 禁止重建硬规则
- Codex 启动不读 board → prompt 第一步硬性指令 + 等 board 就绪

### 4. 🟢 wordlist 缺失（已修复）
- TOOLS.md 承诺 /usr/share/wordlists/dirb/common.txt 不存在（Ubuntu 无 wordlists 包、dirb 路径不同）→ Dockerfile 装 dirb + 符号链接

## 四、行为观察（无需修复，供参考）

1. **Hermes 缺席不影响解题**: 8 题监督死亡但 7 题解出——Hermes 是增强不是依赖，但 guidance 确实帮 Codex 少走弯路（a-03 的 M22 就是例子）
2. **崩溃后 codex 自愈能力强**: 9 题崩溃 8 题解出，说明 codex 在工具调用失败后能自己调整
3. **1.3 轮平均**: MAX_RETRIES=10 冗余充足，大多数题一轮搞定
4. **大日志题多为历史题**: 07-30/31 的题（如 3.8MB 的 2273095efcf）一轮内海量操作但没解出，与昨晚系统无关

## 五、待办

- [ ] push b4670a1（GitHub 凭据缺失，用户醒来处理或配置 credential helper）
- [ ] 重建镜像已触发（含 timeout 修复），完成后 VPN 恢复即可跑
- [ ] 4 个未解出题 VPN 恢复后可通过面板 retry（续跑逻辑已修，上下文会保留）
- [ ] （可选）dashboard 0.0.0.0 无鉴权加固
- [ ] （可选）cred_snapshot model_reasoning_effort 硬编码 high 与宿主同步

## 六、结论

系统整体健康：94% 解出率，主要问题（上下文丢失/监督死锁/工具崩溃）全部定位并修复。
昨晚的崩溃-重试-恢复循环（a-03/f2-05）暴露的 4 类问题已全部修完，VPN 恢复后是验证修复效果的时机。
