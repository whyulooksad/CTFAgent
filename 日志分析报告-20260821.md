# 日志分析报告 - 20260821

> 分析对象: `docs/json/` 下 59 个 session 事件 JSON + 1 个 run 级 CSV (平台日志)
> 分析时间: 2026-08-21
> 背景: 托管模式 (adapter=tsec, backend=process) 打 TSEC 比赛，用户观察到
>       "第二轮只有 hermes 起来、cc 没工作，只有监督者没有解题者"

---

## 一、结论速览

**用户观察到的"第二轮只有 hermes 起来、cc 没工作"现象，经 59 个 session 全量
逐行分析，大部分场景 cc 第二轮是正常工作的 (b-03/b-02/c-03 等均有完整解题
session)；该现象是 3 个因素叠加的**监控链路假象 + 调度窗口**，另有 1 个已修复
的调度 bug 放大了窗口：**

1. **[P0] monitor 触发信号让 hermes 反复"监督空气"**
   - 第二轮重新分发瞬间，claude 启动慢/尚未落盘 codex.log，或 monitor 的
     offset 归零重读旧日志 → monitor 判定"有新进展" → 触发 hermes
   - hermes 读 codex.log 尾部看到的是**第一轮的旧日志** (timestamp 停在首轮)，
     误以为 cc 在推进 → 输出监督意见/更新 board.md → 用户看到"hermes 在输出、
     cc 没动静"
   - 铁证: session 474744 (a-05) 在 21:17 被触发，注入的"最新进展" timestamp
     全是 16:57~17:02 (第一轮早期, 4 小时前)；session 474748 (c-03) 在 21:18
     误以为"c-03 才运行 1 分钟" (实际 4h22m)

2. **[P1] started_round 超时预算漂移 bug（本次已修）**
   - `_round_timeout` 用当前 `current_round` 算预算，长跑题跨圈存活时预算被放大
     (b-01/b-03 4flag: 3360s → 5040s; b-02 6flag: 5040s → 7560s)
   - 导致 b 系多 flag 题占槽时间被拉长 28-42 分钟，排队题 (a-05/c-03 等)
     第二轮迟迟进不了槽 → 用户看到的"第二轮无 cc 动作"大窗口
   - 修复: 新增 `started_round` 字段，超时预算按分发时圈数固定计算
     (已落地 master/challenge_state.py + master/master.py，测试全绿)

3. **[P2] 跨轮次 resume/日志落盘时序竞争（需 run.sh 跟进）**
   - 平台 stop/start 的是靶机实例，agent 容器全程不换，session 文件理论上在
   - 但第二轮 claude 启动存在窗口期 (几秒~几十秒) codex.log 无产出，
     monitor 可能提前触发；极端情况下 resume 失败导致 claude 退出无产出
   - 需 run.sh 增加"cc 启动后 N 秒无 codex.log 产出 → 告警/换新 session"
   - 注: 474730 (c-03 第二轮) 实际工作 292 steps，证明大部分场景 cc 正常，
     该问题仅影响个别轮次的启动窗口

---

## 二、数据基础

### 2.1 文件清单
- `run-11778-events.csv`: 199 行，平台 run 级事件 (任务开始/启动靶场/得分/关闭靶场)
- `session-*.json`: 59 个 agent 会话事件，每个含 session 元信息 + steps (分页)
  - `deepseek-v4-flash` = hermes 监督者 (openai_chat 协议)
  - `deepseek-v4-pro` = cc 主解题者 (anthropic_messages 协议)

### 2.2 时间线对齐
- CSV 时间为北京时间 (UTC+8)，session JSON 为 UTC
- 例: CSV "2026/8/21 03:50:41 启动 B-03" = UTC 2026-08-20T19:50:41
  = session 474704 board.md 里 "Start Time: 2026-08-20T19:50:41+00:00" ✓
- 用户贴的主日志 [18:45:27] ~ [21:02] 为 UTC，与 session 数据同一时段

### 2.3 b 系多 flag 题完整时间线 (CSV 证据)
| 题 | 启动 | 得分 | 关闭 | 实际占槽 |
|----|------|------|------|---------|
| b-01 | 03:50:14 (19:50Z) | 04:43:54 (1/4) | 05:14:35 | 84 min |
| b-03 | 03:50:41 (19:50Z) | 04:00:44 (1/4), 04:25:49 (2/4) | 05:15:02 | 84 min |
| b-02 | 04:09:59 (20:09Z) | 04:26:05 (1/6), 04:26:15 (2/6) | 06:16:41 | 126 min |

→ b-01/b-03 4flag 第一轮预算应为 1200×4×0.7=3360s (56min)，
  本该 04:46 让位，实际 05:14 才关 (多占 28min)
→ b-02 6flag 第一轮预算 1200×6×0.7=5040s (84min)，
  本该 05:34 让位，实际 06:16 才关 (多占 42min)
→ **started_round bug 实锤** (修复后这些时间回到理论值)

---

## 三、关键证据链 (逐 session)

### 3.0 已确认"第二轮 cc 正常工作"的 session (抽查全过)
| session | 目标 | work_dir | 时长 | 状态 |
|---------|------|----------|------|------|
| 474704 | b-03 (10.0.179.146) | manual_web_2f4eb29e8d6b | 21:07-21:22 | 正常: webshell RCE 续攻 flag3/4 |
| 474722 | b-02 (10.0.179.145) | manual_web_c074a9d8999b | 21:11-21:25 | 正常: 内网横向/隧道续攻 |
| 474730 | c-03 (10.0.179.144:3000) | manual_web_42780dbfce68 | 21:15-21:29 | 正常: 识别 Dify/Next.js 探测 |
| 474756 | c-08 (10.0.179.146:7860) | manual_web_642445542382 | 21:21-21:25 | 正常: nmap/curl 识别 Gradio |
| 474728 | a-05 (10.0.179.145) | manual_web_37f1e7926b90 | 21:14-21:21 | 正常: LFI 读源码/API 探测 |

→ **"cc 第二轮普遍罢工"不成立**，用户观察到的窗口是调度/监控假象。

### 3.1 cc (pro) 第二轮 session 正常工作 (474704, b-03)
- first=21:07:10Z, 169 steps, 工作 15min
- 内容: 读 board.md (Start Time 19:50:41 = b-03 首轮), 明确 "We have 2 flags
  already, need flag3/flag4" → 通过 webshell `10.0.179.146/uploads/c.php` 执行命令
  → 分析 SSH pivot 方案
- **结论: b-03 第二轮 cc 正常工作**

### 3.2 cc (pro) 第二轮 session 正常工作 (474722, b-02)
- first=21:11:24Z, 161 steps
- 读 board.md (Start Time 20:09:59 = b-02 首轮), "2 flags found, 4 remaining"
- 检查 webshell (uid=33 正常) + socat 隧道状态 → 继续内网横向
- **结论: b-02 第二轮 cc 正常工作**

### 3.3 hermes 监督空气的铁证 (474744, a-05)
- first=21:17:10Z (UTC), work_dir manual_web_37f1e7926b90 (a-05 合同审批)
- monitor 注入的"主解题 Agent 最新进展" timestamp 全部是 16:57~17:02
  (**第一轮早期，4 小时前**)
- hermes 读 codex.log 尾部 → 看到第一轮的 curl/LFI/读源码内容
  → 输出 "主解题 Agent 方向正常，无需介入" → 更新 board.md
- **实际 cc 本轮无新产出 → hermes 在监督空气**

### 3.4 hermes 时间感知错乱铁证 (474748, c-03)
- first=21:18:27Z, work_dir manual_web_42780dbfce68 (c-03 AI 平台)
- hermes 说 "Now ~16:57, so only ~1 minute elapsed" (实际 21:18 = 4h22m)
- hermes 全局搜 codex.log: 其它 4 个题有，**本题 manual_web_42780dbfce68 没有**
  → "the main agent hasn't produced a codex.log yet"
- **注: 同一 work_dir 的 cc 会话 474730 在 21:15-21:29 实际正常工作了
  292 steps —— hermes 说"无 codex.log"是启动窗口期的时序竞争，
  不是 cc 整体罢工**

### 3.5 subagent 短命 session 澄清 (475016/475020/475021)
- 标题 "Perform a web search for the query: ComfyUI-Manager..."
- working_directory 含 `cc_is_subagent=true` → 是 branch.py 的 subagent
  (查 CVE 的试探进程)，span=0s 属正常短命，**不是 cc 罢工证据**

---

## 四、根因分析

### 4.1 为什么 hermes 会被反复触发 (monitor.py 触发条件)
```
has_new_log / has_flag / is_stale / is_timeout / has_human_guidance /
notify_board_over / branch_changed  (任一为 True 即触发)
```
- `is_stale` (日志停滞>5min) **反而会触发** hermes 介入 → 对，是设计如此
- 但 hermes 被触发后 tail codex.log 读到的**是旧内容** (offset 归零/旧文件)
  → 误判"cc 正常"

### 4.2 为什么 hermes 会读到"旧日志"
- monitor.py 的 `read_log_increment` 用文件 size offset 追踪增量
  (`monitor_state.json` 存 last_log_offset)
- 第二轮 run.sh 重启 (IS_RESUME=1) 保留 codex.log + monitor_state.json，
  但若 codex.log 被覆盖变小 (claude 启动 `> codex.log`) → `size < offset`
  → offset 归零 → **第一轮旧日志被当增量重读** → has_new_log=True → 触发 hermes
- hermes tail codex.log 看到第一轮内容 (timestamp 停在首轮) → 误判 cc 在推进
- 这是 P0 的机制根因

### 4.3 started_round bug 的放大效应
- 第一轮 b 系长跑题占满 3 槽 → 排队题 (a-05/c-03 等) 第二轮迟迟进不了槽
- 20:44 圈推进后预算放大 → b 系继续占槽 → 排队题饿死窗口长达 23min+
- 这期间用户看到"槽位 3/3 死水一潭" + hermes 偶尔输出 (监督残留) →
  强化"第二轮只有 hermes"的观感

### 4.4 补充: cc 第二轮启动窗口期的时序竞争
- 第二轮分发瞬间 claude 启动需要几秒~几十秒才有 codex.log 落盘
- monitor 每 10s 轮询可能提前触发 → hermes 看到"无日志/旧日志"
- 474748 案例即此: hermes 21:18 说 c-03 无 codex.log，但同一 work_dir
  的 cc 会话 474730 在 21:15-21:29 正常工作了 292 steps
- 极个别轮次 resume 失败会导致 claude 退出无产出 (需 run.sh 自愈)

---

## 五、已落地修复 (2026-08-21)

### 5.1 started_round 超时预算修复 ✅
- `master/challenge_state.py`: ChallengeRecord 新增 `started_round` 字段
- `master/master.py`:
  - 分发时记录 `rec.started_round = self.current_round`
  - `_round_timeout` 改用 `started_round` 算 base (兼容旧记录回退 current_round)
- 效果: b-01 第一轮预算固定 3360s，圈推进不再放大；每轮到点让位
- 验证: 全量回归 test_master 9/9 + test_rotation 103/103 + 专项场景全过

### 5.2 待修复 (建议)
- [ ] run.sh: cc 启动后 60s 内 codex.log 无产出 → **告警 + 诊断 + 重试 resume**
      (保持原 session，不换新：claude session 里的上下文/思路/已探测结论不能丢。
      先 tail codex.log 看是否有 init/错误；若 claude 进程活着只是慢 → 继续等；
      若 resume 报 "No conversation found" → 清 .cc_session 重试一次原 sid，
      仍失败才降级 —— 降级也是先读 board.md/progress.md 恢复上下文，再开新会话)
- [ ] monitor.py: 触发 hermes 时注入**真实当前时间 + codex.log mtime**，
      让 hermes 能判断"日志陈旧"而不是误读旧内容；offset 归零重读旧日志时
      跳过 (记录 codex.log 的 mtime/inode，变了才当新日志)
- [ ] monitor.py: `is_stale` 触发时明确标注"日志已停滞 N 分钟"，
      让 hermes 知道是异常状态而非正常推进
- [ ] 平台侧: 确认 agent 容器是否随靶场一起重建；若重建，需持久化
      ~/.claude/projects/ 或 resume 前先验证 sid 存在


---

## 六、结论

"第二轮只有 hermes、cc 没工作"的本质是**监控链路误报 + 调度窗口**的组合假象：
1. hermes 被 monitor 用旧日志/提前触发唤醒，输出监督意见但实际"监督空气"
   (P0) —— 大部分场景 cc 第二轮实际正常工作了 (5 个 session 抽查全过)
2. b 系长跑占槽 + 预算漂移让排队题饿死 → 用户看到"第二轮无 cc 动作"大窗口
   (P1，本次已修: started_round)
3. 个别轮次 cc 启动窗口期 resume/落盘时序竞争 (P2) —— 需 run.sh
   保持原 session 的恢复诊断 (不换新，见 5.2)

started_round bug 已修复并通过全部测试 (test_master 9/9 + test_rotation
103/103 + 专项场景)；P0/P2 需 monitor.py/run.sh 跟进 (见 5.2 建议)。
已构建新镜像 ctf-solver-hosted (含 started_round 修复) 待部署。

